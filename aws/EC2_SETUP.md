# Настройка EC2 для Copfinder

Пошаговая инструкция по настройке EC2 инстанса через AWS Console.

---

## 📋 Что нужно сделать

1. ✅ Создать EC2 инстанс
2. ✅ Настроить Security Group
3. ✅ Развернуть приложение
4. ✅ Настроить переменные окружения

**Время:** 15-30 минут

---

## 1. Создание EC2 инстанса

### Шаг 1.1: Переход в EC2
1. Войдите в [AWS Console](https://console.aws.amazon.com)
2. В поиске введите "EC2" и выберите **EC2**
3. В левом меню выберите **Instances**

### Шаг 1.2: Запуск инстанса
1. Нажмите кнопку **Launch Instance**
2. Заполните форму:

   **Name and tags:**
   - Name: `copfinder-server`

   **Application and OS Images:**
   - Выберите **Amazon Linux 2023** или **Ubuntu Server 22.04 LTS**
   - Оставьте версию по умолчанию

   **Instance type:**
   - Выберите **t3.medium** (2 vCPU, 4 GB RAM)
   - Минимально: **t3.small** (2 vCPU, 2 GB RAM)

   **Key pair:**
   - Если у вас нет ключа, нажмите **Create new key pair**
   - Name: `copfinder-key`
   - Key pair type: **RSA**
   - Private key file format: **.pem**
   - Нажмите **Create key pair** (файл скачается автоматически)
   - **Куда сохранить файл (для Mac):**
     - Файл `copfinder-key.pem` скачается в папку **Загрузки** (Downloads)
     - Откройте **Terminal** (найдите через Spotlight: `Cmd + Space`, введите "Terminal")
     - Выполните следующие команды:
     ```bash
     # Создать папку .ssh, если её нет
     mkdir -p ~/.ssh
     
     # Переместить файл из Загрузок в .ssh
     mv ~/Downloads/copfinder-key.pem ~/.ssh/copfinder-key.pem
     
     # Установить правильные права доступа (важно для безопасности!)
     chmod 400 ~/.ssh/copfinder-key.pem
     ```
     - Готово! Файл теперь находится в `/Users/ваше_имя/.ssh/copfinder-key.pem`
     - `~` означает вашу домашнюю директорию (на Mac это `/Users/ваше_имя/`)

   **Network settings:**
   - VPC: оставьте по умолчанию
   - Subnet: оставьте по умолчанию
   - Auto-assign Public IP: **Enable**
   - Firewall (security groups): **Create security group**
   - Security group name: `copfinder-sg`
   - Description: `Security group for Copfinder application`
   - Inbound rules:
     - Type: **SSH**, Port: **22**, Source: **My IP** (или ваш IP адрес)
     - Нажмите **Add security group rule** для добавления других правил при необходимости

   **Configure storage:**
   - Size: **20 GB** (минимум)
   - Volume type: **gp3**

3. Нажмите **Launch Instance**

### Шаг 1.3: Получение информации об инстансе
1. После создания инстанса нажмите **View all instances**
2. Дождитесь, пока статус изменится на **Running** (это займет 1-2 минуты)
3. **Запишите важную информацию:**
   - **Instance ID** (например: `i-0123456789abcdef0`)
   - **Public IPv4 address** (например: `54.123.45.67`)

---

## 2. Настройка Security Groups

### Шаг 2.1: Редактирование Security Group
1. В EC2 Console выберите **Security Groups** в левом меню
2. Найдите `copfinder-sg` и выберите его
3. Перейдите на вкладку **Inbound rules**
4. Нажмите **Edit inbound rules**

### Шаг 2.2: Проверка правил
- **SSH (22)**: должен быть добавлен для вашего IP
- Для доступа к приложению через веб (если планируете):
  - Type: **Custom TCP**
  - Port: **3000** (или другой порт)
  - Source: **My IP**

5. Нажмите **Save rules**

---

## 3. Развертывание приложения

### Шаг 3.1: Подготовка переменных окружения
Откройте Terminal на вашем Mac и выполните:

```bash
# Перейдите в директорию проекта
cd /Users/ff/Desktop/Copfinder

# Вариант 1: Если у вас есть права AWS CLI
export AWS_INSTANCE_ID="i-xxxxxxxxxxxxx"  # Замените на ваш Instance ID
export AWS_SSH_KEY="~/.ssh/copfinder-key.pem"
export AWS_SSH_USER="ec2-user"  # или "ubuntu" для Ubuntu

# Вариант 2: Если нет прав AWS CLI (указывайте IP напрямую)
export AWS_PUBLIC_IP="54.235.6.145"  # Замените на ваш Public IP из шага 1.3
export AWS_SSH_KEY="~/.ssh/copfinder-key.pem"
export AWS_SSH_USER="ec2-user"  # или "ubuntu" для Ubuntu
```

**Если получили ошибку о правах IAM:** См. [IAM_PERMISSIONS_FIX.md](IAM_PERMISSIONS_FIX.md) или используйте Вариант 2 (IP напрямую).

### Шаг 3.2: Запуск скрипта развертывания
```bash
# Сделайте скрипт исполняемым (если еще не сделано)
chmod +x aws/ec2-deploy.sh

# Запустите скрипт развертывания
./aws/ec2-deploy.sh
```

Скрипт автоматически:
- ✅ Создаст архив проекта
- ✅ Загрузит его на сервер
- ✅ Установит Node.js и зависимости
- ✅ Установит Playwright и браузеры
- ✅ Настроит PM2 для автозапуска

**Время выполнения:** 5-10 минут

---

## 4. Настройка переменных окружения

### Шаг 4.1: Подключение к серверу
```bash
# Подключитесь к серверу (замените YOUR_PUBLIC_IP на IP из шага 1.3)
ssh -i ~/.ssh/copfinder-key.pem ec2-user@YOUR_PUBLIC_IP

# Если используете Ubuntu, замените ec2-user на ubuntu:
# ssh -i ~/.ssh/copfinder-key.pem ubuntu@YOUR_PUBLIC_IP
```

### Шаг 4.2: Настройка .env файла
```bash
# Перейдите в директорию приложения
cd /opt/copfinder

# Создайте .env файл из примера
cp config/env.example .env

# Откройте файл для редактирования
nano .env
```

Заполните следующие переменные:
```env
COPART_TELEGRAM_BOT_TOKEN=ваш-токен-бота-copart
COPART_TELEGRAM_CHAT_ID=ваш-chat-id-copart
TELEGRAM_BOT_TOKEN=ваш-токен-бота-iaai
TELEGRAM_CHAT_ID=ваш-chat-id-iaai
```

**Как получить токены Telegram:**
1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Получите Chat ID через [@userinfobot](https://t.me/userinfobot) или [@getidsbot](https://t.me/getidsbot)

После заполнения:
- Нажмите `Ctrl + X` для выхода
- Нажмите `Y` для сохранения
- Нажмите `Enter` для подтверждения

### Шаг 4.3: Запуск приложения
```bash
# Перезапустите приложение через PM2
pm2 restart ecosystem.config.cjs

# Сохраните конфигурацию PM2 для автозапуска
pm2 save
```

---

## 5. Проверка работы

### Шаг 5.1: Проверка статуса
```bash
# Проверьте статус процессов
pm2 status

# Должны быть запущены два процесса:
# - copfinder-copart
# - copfinder-iaai
```

### Шаг 5.2: Просмотр логов
```bash
# Просмотр всех логов
pm2 logs

# Просмотр логов конкретного процесса
pm2 logs copfinder-copart
pm2 logs copfinder-iaai

# Просмотр логов в реальном времени
pm2 logs --lines 50
```

### Шаг 5.3: Проверка работы
- Проверьте, что приложение отправляет уведомления в Telegram
- Проверьте логи на наличие ошибок
- Убедитесь, что процессы работают стабильно

---

## 6. Управление приложением

### Полезные команды PM2:

```bash
# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs

# Перезапуск всех процессов
pm2 restart ecosystem.config.cjs

# Перезапуск конкретного процесса
pm2 restart copfinder-copart
pm2 restart copfinder-iaai

# Остановка
pm2 stop ecosystem.config.cjs

# Удаление из PM2
pm2 delete ecosystem.config.cjs

# Мониторинг в реальном времени
pm2 monit
```

### Обновление приложения:

```bash
# На вашем локальном Mac:
cd /Users/ff/Desktop/Copfinder
./aws/ec2-deploy.sh

# На сервере (после развертывания):
cd /opt/copfinder
pm2 restart ecosystem.config.cjs
```

---

## Troubleshooting

### Проблема: Не могу подключиться к EC2
- **На Mac:** Убедитесь, что файл ключа находится в `~/.ssh/` и имеет правильные права:
  ```bash
  ls -la ~/.ssh/copfinder-key.pem  # Должен показать права -r--------
  chmod 400 ~/.ssh/copfinder-key.pem  # Если права неправильные
  ```
- Проверьте Security Group: должен быть открыт порт 22 для вашего IP
- Проверьте, что инстанс в статусе "Running"
- Убедитесь, что используете правильный пользователь:
  - `ec2-user` для Amazon Linux
  - `ubuntu` для Ubuntu Server
- Проверьте правильность IP адреса в EC2 Console

### Проблема: Приложение не запускается
- Проверьте логи: `pm2 logs`
- Убедитесь, что `.env` файл заполнен правильно
- Проверьте, что все зависимости установлены: `npm list`
- Проверьте права доступа к файлам: `ls -la /opt/copfinder`

### Проблема: Playwright не работает
- Установите дополнительные зависимости:
  ```bash
  sudo yum install -y nss atk at-spi2-atk cups-libs gtk3 libXcomposite libXcursor
  ```
- Переустановите браузеры: `npx playwright install --with-deps chromium`

### Проблема: Недостаточно памяти
- Увеличьте размер инстанса в EC2 Console
- Или настройте swap файл:
  ```bash
  sudo dd if=/dev/zero of=/swapfile bs=1M count=2048
  sudo chmod 600 /swapfile
  sudo mkswap /swapfile
  sudo swapon /swapfile
  ```

---

## Полезные ссылки

- [EC2 Console](https://console.aws.amazon.com/ec2/)
- [PM2 Documentation](https://pm2.keymetrics.io/)
- [Playwright Documentation](https://playwright.dev/)

---

## Следующие шаги

1. ✅ Настройте мониторинг (опционально)
2. ✅ Настройте автоматические бэкапы (опционально)
3. ✅ Настройте CloudWatch алерты (опционально)

**Готово!** Ваше приложение должно работать на EC2. 🎉

