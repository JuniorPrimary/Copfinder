# Проверка Security Group для подключения к EC2

## 🔍 Проблема: Connection timed out

Если вы получаете ошибку "Connection timed out", это обычно означает проблему с Security Group.

---

## ✅ Пошаговая проверка Security Group

### Шаг 1: Откройте AWS Console
1. Войдите в [AWS Console](https://console.aws.amazon.com)
2. Перейдите в **EC2** → **Instances**
3. Найдите инстанс с IP `54.235.6.145`
4. Кликните на инстанс, чтобы выделить его

### Шаг 2: Проверьте Security Group
1. В нижней панели найдите секцию **Security**
2. Кликните на **Security groups** (например, `sg-xxxxx`)
3. Откроется страница Security Group

### Шаг 3: Проверьте Inbound Rules
1. Перейдите на вкладку **Inbound rules**
2. Проверьте, есть ли правило для SSH:

   **Должно быть:**
   - **Type:** SSH
   - **Protocol:** TCP
   - **Port range:** 22
   - **Source:** Ваш IP адрес или `My IP`

### Шаг 4: Добавьте правило (если его нет)
1. Нажмите **Edit inbound rules**
2. Нажмите **Add rule**
3. Заполните:
   - **Type:** SSH
   - **Source:** 
     - Выберите **My IP** (автоматически определит ваш IP)
     - Или выберите **Custom** и введите ваш IP вручную
4. Нажмите **Save rules**

---

## 🔧 Альтернативные решения

### Решение 1: Временно открыть для всех IP (НЕ рекомендуется для продакшена)
**Только для тестирования!**

1. В Inbound rules добавьте:
   - **Type:** SSH
   - **Source:** `0.0.0.0/0` (все IP)
2. Попробуйте подключиться
3. **После тестирования удалите это правило!**

### Решение 2: Проверьте статус инстанса
1. В EC2 Console проверьте статус инстанса
2. Должен быть **Running** (зеленый)
3. Если **Stopped** - запустите его (Actions → Start instance)

### Решение 3: Проверьте правильность IP
1. В EC2 Console найдите ваш инстанс
2. Проверьте **Public IPv4 address**
3. Убедитесь, что это `54.235.6.145`
4. Если IP другой - используйте новый IP

---

## 📋 Чеклист

- [ ] Инстанс в статусе "Running"
- [ ] Security Group имеет правило для SSH (порт 22)
- [ ] Source в правиле - ваш IP или "My IP"
- [ ] Правильный IP адрес (`54.235.6.145`)
- [ ] Правильный пользователь (`ec2-user` или `ubuntu`)

---

## 🧪 Тест после настройки

После настройки Security Group попробуйте:

```bash
# Для Amazon Linux:
ssh -i ~/.ssh/copfinder-key.pem ec2-user@54.235.6.145

# Для Ubuntu:
ssh -i ~/.ssh/copfinder-key.pem ubuntu@54.235.6.145
```

---

## 💡 Как узнать ваш текущий IP

```bash
# В терминале Mac:
curl ifconfig.me

# Или через веб:
# Откройте https://whatismyipaddress.com/
```

Затем добавьте этот IP в Security Group как **Custom** source.

