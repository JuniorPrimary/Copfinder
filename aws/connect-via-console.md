# Подключение к EC2 через AWS Console

## 🔧 Проблема: SSH не подключается напрямую

Если SSH не работает напрямую, можно подключиться через AWS Console.

---

## ✅ Способ 1: Через кнопку "Connect" в AWS Console

### Шаг 1: Откройте инстанс
1. В AWS Console перейдите в **EC2** → **Instances**
2. Найдите инстанс `i-0c2ea1e5ebbcd889e` (copfinder-server)
3. Выделите его

### Шаг 2: Подключение
1. Нажмите кнопку **Connect** (вверху справа)
2. Выберите вкладку **SSH client**
3. Скопируйте команду подключения
4. Выполните её в терминале

**Или:**

1. Нажмите **Connect** → **EC2 Instance Connect**
2. Нажмите **Connect**
3. Откроется терминал в браузере

---

## ✅ Способ 2: Проверка SSH сервиса на инстансе

Если инстанс только что запущен, SSH сервис может еще не быть готов.

**Подождите 2-3 минуты** после запуска инстанса, затем попробуйте снова.

---

## ✅ Способ 3: Использование правильного ключа

Убедитесь, что используете правильный ключ для этого инстанса:

1. В AWS Console → EC2 → Instances
2. Выделите инстанс
3. Внизу найдите **Key pair name**
4. Убедитесь, что у вас есть файл с этим именем

---

## 🔍 Диагностика

### Проверьте логи инстанса:
1. В AWS Console → EC2 → Instances
2. Выделите инстанс
3. Actions → Monitor and troubleshoot → **Get system log**
4. Проверьте, нет ли ошибок при запуске

### Проверьте screenshot:
1. Actions → Monitor and troubleshoot → **Get instance screenshot**
2. Посмотрите, что происходит на экране инстанса

---

## 💡 Альтернатива: Пересоздать инстанс

Если ничего не помогает, можно:
1. Создать новый инстанс с тем же образом
2. Использовать тот же Security Group
3. Использовать тот же ключ

---

## 📝 После успешного подключения

Как только подключитесь, выполните:

```bash
# Проверьте статус SSH
sudo systemctl status sshd

# Если не запущен, запустите:
sudo systemctl start sshd
sudo systemctl enable sshd

# Проверьте конфигурацию SSH
sudo cat /etc/ssh/sshd_config | grep -E "^(Port|PermitRootLogin|PasswordAuthentication)"
```

